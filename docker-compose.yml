# Standalone client build for local dev. The real stack (portal + auth + games)
# runs from ~/Desktop/game/docker-compose.yml — auth/scores don't work here.
services:

  client:
    build:
      context: .
      dockerfile: BulletHeaven.Client/Dockerfile
    ports:
      - "8080:80"
